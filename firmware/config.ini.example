# CYD Palm runtime config -- copy to the SD card as /sdcard/config.ini
#
# Parsed by bridge/config.c (config_load). Settings on the device reads and
# writes these values, so Wi-Fi, iCloud and the per-app collections can change
# without reflashing -- and most people never need to open this file at all.
#
# Format: `key = value`, one per line. '#' starts a comment. Surrounding
# whitespace is ignored; unknown keys and malformed lines are skipped.
#
# PASSWORDS: you may type the Wi-Fi and iCloud app-specific passwords here, but
# they do not stay: on its next boot the device moves them into its own flash
# and rewrites this file without them, so the card never carries them for long.
# The firmware never logs them.

wifi_ssid = your-ssid
wifi_pass = your-wifi-password

dav_user  = you@icloud.com
dav_pass  = xxxx-xxxx-xxxx-xxxx          # iCloud APP-SPECIFIC password (with dashes)
dav_base       = https://caldav.icloud.com
dav_card_base  = https://contacts.icloud.com

# collection paths from `bridge_cli discover` (drop the leading '/'); empty = skip
cal_coll  = 21226414825/calendars/REPLACE-WITH-CALENDAR-UUID
todo_coll =
card_coll =

timezone      = America/New_York          # empty = floating local time

# Weather location, decimal degrees (east/north positive). Empty = no weather.
# Forecast resolution is kilometres, so a rough figure for your town is plenty.
latitude      = 40.7143
longitude     = -74.0060

brightness    = 80                        # backlight 0..100
backlight_sec = 30                        # dim after N idle seconds (0 = never)
policy        = server                    # conflict policy: server | local | both
